[project]
name = "friedman-net"
version = "0.1.0"
description = "Generational evolutionary training with economic agents"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyterlab>=4.5.0",
    "torch>=2.0.0",
    "torchvision>=0.15.0",
]
